export const metadata = {
  title: "Sign Up | Cicada Music Society",
  description: "Create an account with Cicada Music Society to purchase tickets and access exclusive member benefits.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function SignUpLayout({ children }) {
  return <>{children}</>;
}

