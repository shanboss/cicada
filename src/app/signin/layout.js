export const metadata = {
  title: "Sign In | Cicada Music Society",
  description: "Sign in to your Cicada Music Society account.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function SignInLayout({ children }) {
  return <>{children}</>;
}

