export const metadata = {
  title: "Profile | Cicada Music Society",
  description: "View and manage your profile information.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function ProfileLayout({ children }) {
  return <>{children}</>;
}

