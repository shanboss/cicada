export const metadata = {
  title: "Verify Tickets | Cicada Music Society",
  description: "Staff portal for verifying and checking in event tickets.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function VerifyLayout({ children }) {
  return <>{children}</>;
}

