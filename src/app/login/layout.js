export const metadata = {
  title: "Login | Cicada Music Society",
  description: "Sign in to your Cicada Music Society account to access your tickets and member benefits.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function LoginLayout({ children }) {
  return <>{children}</>;
}

