export const metadata = {
  title: "Admin Dashboard | Cicada Music Society",
  description: "Admin dashboard for managing events and tickets.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function AdminLayout({ children }) {
  return <>{children}</>;
}

