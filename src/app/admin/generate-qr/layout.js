export const metadata = {
  title: "Generate Admin QR Code | Cicada Music Society",
  description: "Generate QR codes for tickets without purchase.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function AdminGenerateQRLayout({ children }) {
  return <>{children}</>;
}
