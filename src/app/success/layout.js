export const metadata = {
  title: "Payment Successful | Cicada Music Society",
  description: "Your payment has been processed successfully.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function SuccessLayout({ children }) {
  return <>{children}</>;
}

