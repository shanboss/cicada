export const metadata = {
  title: "Order Confirmation | Cicada Music Society",
  description: "Your ticket purchase has been confirmed. View your tickets and QR codes here.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function ReturnLayout({ children }) {
  return <>{children}</>;
}

